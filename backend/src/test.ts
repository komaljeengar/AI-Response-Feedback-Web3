/*import RewardSystem from './rewardSystem'; // Ensure this path is correct
import MockBlockchainService from './MockBlockchainService'; // Path to your mock

// Create an instance of RewardSystem, passing the mock BlockchainService
const rewardSystem = new RewardSystem(new MockBlockchainService());

// Test feedback processing
rewardSystem.processFeedback("user123", "This product is amazing, I love it!", 5)
    .then(result => console.log(result))
    .catch(err => console.error(err)); */

